# Tech debt

Baselines stay empty: `app/lint-baseline.xml` and `app/detekt-baseline.xml`.
`./gradlew :app:lintDebug` fails on errors; `lintVitalRelease` is clean.

**Still open warnings** (do not baseline, do not `warningsAsErrors` until decided):

- `OldTargetApi` — disabled in `app/build.gradle.kts` lint config until a
  deliberate `targetSdk` 36→37 bump PR. Do not re-enable casually.

Size and complexity findings are silenced with targeted `@file:Suppress`
rather than a baseline — prefer extracting over widening those lists. As of
2026-09-23 that is not "a few files": 81 main files carry `@file:Suppress`
(most often `MagicNumber` 55, `ReturnCount` 29, `TooManyFunctions` 27,
`LongParameterList` 21, `CyclomaticComplexMethod` 19, `LongMethod` 18) and 17
carry `@file:SuppressLint`. Since the baselines are empty, suppression is the
only thing keeping those findings quiet.
Catalog version-availability lint IDs are disabled; bump deps in deliberate PRs.

`UnclosedTrace`, `PluralsCandidate`, `UseKtx`, `TooManyViews` on
`activity_settings` / `wizard_step_settings`, and the 2026-08 lint warning
set are fixed (content extracted behind `SettingsScrollContentView` /
`WizardStepSettingsContentView`). The architecture extracts that had missed
`main` (#65 / #67, re-landed as #69 / #70) are on `origin/main` as of
2026-08-16.

Orphaned strings the 2026-08-18 workflow audit found are listed in
[../app/WORKFLOWS.md](../app/WORKFLOWS.md) §11 — none of them fail a gate, so they
are removed opportunistically rather than in a sweep.

## Proposed improvements live next door

Forward-looking items are in [FUTURE_IMPROVEMENTS.md](FUTURE_IMPROVEMENTS.md).
This file stays the record of what is *owed* and what is deliberately deferred.

## Open register

Priority = (Impact + Risk) × (6 − Effort).

**Re-verified against the code on 2026-09-23.** Every row below was checked at
`main` @ `4d04c28`, not copied from the previous register; several of the old
rows' numbers were wrong and are corrected here. Evidence is `path:line`.
Architecture decisions for the structural rows are in [../adr/](../adr/README.md);
the remediation order is fourteen PRs, tracked in the Status column.

### Carried over (re-scored)

| ID | Category | Item | I | R | E | P | Status |
|----|----------|------|---|---|---|---|--------|
| TD-3 | Architecture | The viewer's read side: `ViewerArgs` (`ui/viewer/ViewerArgs.kt`) is the one writer, but four readers (`ResultViewerActivity` 25 `DicKeys` refs, `VsgLatticeActivity` 21, `ViewerSettingsSheet` 12, `ViewerReportFactory` 8) parse the bundle themselves with **different silent defaults** (subset 41 vs 0, step 5 vs 1, ROI image-size vs 0). The lattice→viewer hop copies the whole bundle (`VsgLatticeActivity.kt:757-764`) | 3 | 3 | 3 | **18** | **Fixed** (viewer-args PR, [ADR-003](../adr/ADR-003-viewerargs-read-side.md)): the four readers take `ViewerArgs.from(intent, record)`; one default per field; the lattice hop re-packs. `ViewerArgsTest` (11), `ViewerEntryParityDeviceTest` (2) |
| TD-24 | Architecture | No `@MainThread` on UI entry points (0 in `app/src/main`), so `SessionStore`'s 13 `@WorkerThread` annotations are not a gate. **14** Activities (the old row said ~27) | 2 | 2 | 2 | **16** | **Fixed** (app-reuse PR): all 14 Activities carry a class-level `@MainThread`, so lint's `WrongThread` now checks every `SessionStore` `@WorkerThread` call made from them. It found one site, `SettingsActivity.enqueueRestoreWithStub`, which already ran on the IO dispatcher and is now annotated `@WorkerThread`. A helper that runs off the main thread has to say so with `@WorkerThread` or `@AnyThread` |
| TD-25 | Test | `AuthRepository` cannot be unit-tested: `IndicApi` is `class IndicApi private constructor` (`data/net/IndicApi.kt:44`), reached by 21 `IndicApi.get(` calls in 10 files; no mocking library in the test stack | 2 | 2 | 2 | **16** | **Fixed** (cloud-api-seam PR, [ADR-002](../adr/ADR-002-cloudapi-seam.md)): `IndicApi` implements `CloudApi`; `AuthRepository`, `SeatLease`, `CloudSync` and `CloudAccountExport` take it as a defaulted parameter. `AuthRepositoryTest` (14) and `CloudSeamTest` (13) run on `FakeCloudApi` |
| TD-26 | Architecture | **Reframed.** Rotation does not recreate the wizard (full `configChanges`, `AndroidManifest.xml:147-149`). **Process death** loses everything: no `SavedStateHandle` anywhere, and `CacheJanitor` deletes `temp_deformed` on start (`data/CacheJanitor.kt:132`). A re-run after a kill loses `workingLocalId` and creates a second Home row | 3 | 3 | 3 | **18** | **Fixed** (wizard-draft PR, [ADR-005](../adr/ADR-005-wizard-process-death.md)): scalars in `SavedStateHandle`, reference/mask/frame list in `filesDir/wizard_draft/`, `CacheJanitor` keeps a live draft's `temp_deformed`, restore is all-or-nothing with a snackbar. `WizardStateTest` (8), `WizardDraftRestoreTest` (2, emulator), scripted kill pass |
| TD-27 | Ops | The API Gateway is never deployed by CI; the runbook ([BACKEND_SETUP_GCP.md](../backend/BACKEND_SETUP_GCP.md) "Redeploying the gateway") also had three faults (unset `FIREBASE_PROJECT_ID`, same-day config-id collision, inverted `grep && exit 1`) | 3 | 3 | 3 | **18** | **Fixed** (2026-09-24, [ADR-006](../adr/ADR-006-gateway-deploy-job.md)): the `gateway` job in `deploy-backend.yml` (dry-run by default; apply creates, switches, verifies, rolls back) and a rewritten runbook. The first dry-run (run 35963412251) was denied `apigateway.apis.get` before the IAM grant; the second (35990981043) failed decoding the live document with GNU `base64 -d`, fixed in #183 (`deploy-backend.yml:435-450`). The third (35991624707) diffed cleanly, and `apply` (35992296245) created `v202609241122-44`, switched `semper-gw`, and verified `/v1/config` → 401 and preflight → 200 |
| TD-28 | Backend | A per-user `maxSessions` override is ignored while the account is demo (`backend/app/firestore_repo.py:537-538`) | 2 | 2 | 2 | 16 | **Closed as decided (2026-09-23):** "lift a demo cap" means attach a licence. `BACKEND_SETUP_GCP.md` described it wrongly and is corrected |
| TD-29 | App | Two auth continue hosts (`AUTH_HOST` / `LEGACY_AUTH_HOST`, `data/AuthRepository.kt:47,55`; four App Link hosts in the manifest; `AuthHostsTest.kt:17` pins the legacy host) | 1 | 2 | 2 | **8** | Deferred until Play vitals show no legacy-only build installed |
| TD-30 | Ops | API Gateway in asia-northeast1 vs Cloud Run in asia-south1. Whether the legacy `indic-gw` / `indic-api` pair still exists **cannot be verified from the repo**, and CONTEXT previously said it was deleted: check with `gcloud api-gateway gateways list` and `gcloud run services list` | 2 | 1 | 3 | **9** | **Accepted** (checked 2026-09-24). The legacy pair is gone: `gcloud api-gateway apis list` shows only `semper-api` and `semper-api-staging`, `gcloud run services list` only `semper-api` and `semper-api-staging`, and `gcloud endpoints services list` has no `indic-api` service. API Gateway offers no asia-south1 location (its locations API lists asia-northeast1 as the only Asian region), so the gateway cannot move next to Cloud Run. Measured over 14 days (`serviceruntime.googleapis.com/api/request_latencies_*`, service `semper-api`): gateway overhead p50 135 ms / p95 1.0 s, backend p50 0.71 s / p95 1.7 s, where backend includes the Tokyo→Mumbai leg. Removing the hop means replacing API Gateway, which needs an ADR, not a fix. **2026-09-26 (cost review):** kept, because the gateway costs nothing at 1,492 calls in 30 days. Production now scales to zero, so a cold start (p50 3.9 s, p95 6.0 s) matters more than this hop. Two options are deferred until a latency complaint or a public launch: (a) new builds call Cloud Run directly, and the gateway is retired once its traffic is about zero; (b) a global HTTPS load balancer with a serverless NEG, about $20–25 a month. Both need an app release ([perf/backend-cost.md](../perf/backend-cost.md)) |
| TD-33 | Backend | A contended invite claim that starves out leaves the account on the Demo key it then mints; `claim_pending_invite` returns early on `licenseId` (`firestore_repo.py:2192`), so the invite stays pending. The client retries **10** times (`_TX_ATTEMPTS`, `firestore_repo.py:49`), not five as previously written | 2 | 1 | 3 | **9** | **Fixed** (backend quick-wins PR): `ensure_entitlement` skips the Demo mint when the claim failed with `_CONTENDED`, so the invite stays claimable and the next request takes it; a non-transient failure (seats exhausted) still mints Demo. `test_a_starved_invite_claim_mints_no_demo_key` fails on the old code |
| TD-34 | Console | Dead pre-#129 enrolment card on the operator desk (`operator/index.html:44-73`, `operator.js:14,54-90`) **and** the account page (`account/index.html:47-77`, `account.js:19,30-63`); `auth.js:472` awaits `ensureDashboardMfa()` before any page code runs, so it never shows | 1 | 2 | 2 | **12** | **Fixed** (console PR): both enrolment cards and their handlers are gone; the pages show a fixed "2FA on" pill, since `ensureDashboardMfa` has already enrolled and confirmed a factor before page code runs. `check_console.py` now passes on Windows too (it fed node cp1252) |

### New (found 2026-09-23)

| ID | Category | Item | I | R | E | P | Status |
|----|----------|------|---|---|---|---|--------|
| TD-35 | Infra / privacy | The monthly restore drill purges only `users devices sessions files audit_logs challenges _migrations` (`.github/workflows/firestore-restore-drill.yml:96`), so `licenses`, their `seats`, `licenseInvites` and `auth_links` — production PII — stay in the weaker drill project; `scripts/firestore_verify.py` does not check them either | 3 | 4 | 1 | **35** | **Fixed** (CI/ops PR): drill purges `licenses seats licenseInvites auth_links` too; `firestore_verify.py` counts them (`seats` as a collection group) |
| TD-36 | Legal | 10 bracketed operator fields in `docs/legal/PRIVACY_POLICY.md` (:5 ×2, :7 ×2, :18, :153) and `TERMS_OF_SERVICE.md` (:6 ×2, :438, :439) are published as literal text on the hosted pages | 2 | 5 | 1 | **35** | Operator task (values); listed in [PRODUCTION_READINESS_GATE.md](PRODUCTION_READINESS_GATE.md) |
| TD-61 | **Accuracy** | The viewer's metadata depends on how it was opened. A fresh run passes the **requested** ROI (`ui/analysis/AnalysisNavHelper.kt:113-116`) but the session saves the **resolved, inset** ROI the engine ran on (`ui/analysis/DicBatchRunner.kt:340-343`), so the report, ⓘ sheet, CSV header and lattice line cut change when the same session is reopened from Home. Also `SESSION_ID` (`Pending_Cloud_Sync_…` vs the local id), sweep subset/strain window (slider vs first combination), and `lastPlannedFrames` / `lastStep` left stale by an all-failed sweep | 3 | 4 | 2 | **28** | **Fixed.** Writer side in the run-spec PR ([ADR-004](../adr/ADR-004-runspec.md)), reader side in the viewer-args PR ([ADR-003](../adr/ADR-003-viewerargs-read-side.md)). On the emulator the viewer opened from the run and from Home parses equal arguments (bar the run's own image paths) and shows the same ⓘ rows (`ViewerEntryParityDeviceTest`) |
| TD-37 | Build | No `ndkVersion` (`app/build.gradle.kts`): the bit-exact `-ffast-math` engine silently follows AGP's default NDK | 2 | 3 | 1 | **25** | **Fixed** (CI/ops PR): `ndkVersion = "28.2.13676358"`, the NDK every local `.cxx` build already used |
| TD-38 | Test | The Kover floor is configured (`minBound(27)`, `app/build.gradle.kts:389-395`) but **never enforced**: CI runs `koverLog` only (`ci.yml:294-295`) and `ciReleaseGate` omits `koverVerify`. Measured 31.9 % locally | 2 | 3 | 1 | **25** | **Fixed** (CI/ops PR): `koverVerify` in tier 1 and `ciReleaseGate`; 32.1 % measured |
| TD-39 | Test | `DicResultCsvTest.kt:33` pins `DicResult.CSV_POINT_HEADER` (`DicResult.kt:177`), but production CSV uses its own copy (`report/AnalysisCsvWriter.kt:49`) | 2 | 3 | 1 | **25** | **Fixed** (app-correctness PR): `AnalysisCsvWriter` uses `DicResult.CSV_POINT_HEADER`; one definition |
| TD-40 | Infra | R8 `mapping.txt` is a 90-day workflow artifact, deliberately not attached to the release (`release.yml:231-240`), and Crashlytics mapping upload is off: field crashes become unreadable after 90 days | 2 | 3 | 1 | **25** | **Fixed** (CI/ops PR): release builds pass `-PuploadCrashlyticsMapping=true`, so Crashlytics keeps the mapping privately; the artifact stays as a 90-day fallback. First real upload happens on the next release |
| TD-41 | Code | `runCatching` around a suspend call swallows `CancellationException` at **21** sites (`AuthRepository` ×5, `CloudRestore:563`, `CloudSync:153,268`, `DicUploadWorker` ×7, `SeatLease:64,86,103`, `SeatRequiredActivity:69`, `SettingsYourDataSection:116`, `AuthActivity:428`). Visible bug: cancelling the cloud account export shows "export failed" and logs `EXPORT_FAILED` | 3 | 3 | 2 | **24** | **Fixed** (app-correctness PR): `util.suspendRunCatching` rethrows cancellation at all 21 sites. The local export also swallowed cancellation (`SessionEverythingExporter` caught every `Exception` and never checked between sessions) and kept packing after Cancel; `LocalExportCancelTest` failed on the old code (3 sessions after a cancel) and passes now. `SessionUploadBundler` now checks per frame and per GIF, so Cancel on the emulator clears the banner in about 2 s instead of 4–6 s |
| TD-42 | CI / security | Dispatch inputs interpolated into shell (`deploy-backend.yml:74,76`; `backend-lock.yml:99,127`); no top-level `permissions:` in `ci.yml`; no `refs/heads/main` guard on the deploy; `reactivecircus/android-emulator-runner@v2` unpinned at `ci.yml:668` (SHA-pinned at `:357`); `pip-audit` / `pip-tools` unpinned; gitleaks downloaded by curl with no checksum (`ci.yml:57-59`) | 2 | 3 | 2 | **20** | **Fixed** (CI/ops PR): inputs via `env:`, `ci.yml` read-only default, production deploy only from `main`, runner SHA-pinned, `pip-audit==2.10.1` / `pip-tools==7.6.1`, gitleaks tarball checked against the release checksums |
| TD-43 | CI | The backend gate differs across three workflows: `deploy-backend.yml:61-62` runs no pip-audit, coverage gate, emulator tier or ruff on scripts; `release.yml:65` runs no lint/detekt/spotless before signing | 2 | 3 | 2 | **20** | **Fixed** (CI composites PR): `.github/actions/backend-gate` is the one backend gate for CI tier 4, Release and Deploy; Release's `verify-android` runs tier 1's spotless/detekt/lint. The Firestore emulator tier stays CI-only: it needs Node and Java, and production ships only from `main`, where CI tier 4 runs it on every backend change |
| TD-44 | Backend / observability | `observability.classify_route` (`backend/app/observability.py:24,37-41`) rewrites any unlisted segment of 8+ characters to `{id}`: `/v1/licenses/activate` logs as `/v1/{id}/{id}` and every licence and institution route is opClass `other`. Blocks the telemetry that shim retirement needs | 3 | 2 | 2 | **20** | **Fixed** (backend quick-wins PR): `main.py` registers every router's declared `path_format`; `classify_route` matches against those (params → `{id}`) and falls back to the id regex only for undeclared paths. New opClasses `license` and `institution`; `/v1/me/consents` and `/v1/me/terms` are `account`. `test_every_declared_route_has_a_class` fails on a route left as `other` |
| TD-45 | Backend | Compat shims from the `campus`→`institution` / `plan`→`mode` rename (CLOUD_ARCHITECTURE_GCP §20.5). Four remain, all waiting on the installed-app fleet, which cannot be measured yet: (6) the app reads `config.plan` when `mode` is empty (`AppRemoteConfig.kt:203-206`, `ApiDtos.kt:75`); (7) the app falls back to the old `plan` pref key (`AppRemoteConfig.kt:220-224`); (8) the backend writes a `plan` mirror into `/v1/config`, licence summaries and user documents (`repo/_base.py:123-131`, `repo/user_config.py:199`, `repo/mint.py:61,126`, `licenses.py:81`); (9) `normalize_mode` / `normalize_kind` read `plan` / `campus` values in stored documents (`licenses.py:86,98`) | 2 | 2 | 2 | **16** | **Partly fixed.** Backend quick-wins PR: the never-called `/v1/campus/*` invite-revoke alias deleted; `LICENSE_ACTIVATE` audits `mode`. 2026-09-26: shims 1–5 retired on signals checked live that day — `PRO_MAX_SESSIONS_PER_USER` read (on neither Cloud Run service), the ID-token-only `/uploads` read and `REQUIRE_ATTESTED_UPLOADS` (0 `legacy_unattested_uploads` in 30 days of logs), `kind="campus"` at mint and the `plan` config patch (now 422s; no tooling sends them, 0 calls to the route), and the four `/v1/campus/*` seat routes in the router and gateway spec (0 requests in 30 days). Open: 6–9. Signals — 6/7/8: Play Console version distribution shows a `mode`-reading build is the fleet (6 ships with 7); 9: migration 002 has reached every document and #8 stopped writing the mirror (a Firestore count of users with no `mode` and licences with `kind == "campus"` is zero) |
| TD-46 | Dependencies | Dependabot (`.github/dependabot.yml`) has no `docker` (the digest-pinned `backend/Dockerfile`), `gitsubmodule` (`native`) or `.github/actions/*` entries; CI uses Node 20 (EOL April 2026) and `firebase-tools@13`; the Firestore workflows pin `google-cloud-firestore==2.21.0` against the backend's 2.29.0 | 2 | 2 | 1 | **20** | **Fixed** (CI/ops PR): Dependabot `docker` (`/backend`) and `/.github/actions/*`; Node 22; `firebase-tools` exact (`15.31.0` on Java 21 since 2026-09-24); the Firestore workflows' `google-cloud-firestore` pin tracks `backend/requirements.txt` (2.31.0 since #173). No `gitsubmodule`, deliberately: an engine pin move is a reviewed oracle change |
| TD-47 | Security | `.gitleaks.toml` allowlists every `local.properties` (:14), two paths that no longer exist (:15-16) and four whole commits (:18-23) | 1 | 3 | 1 | **20** | **Fixed** (CI/ops PR): the `local.properties` and dead website-sync path rules are gone. A full-history scan (764 commits, CLI 8.24.3 and 8.28.0) then showed the `SupabaseManager.kt` path and commits `9a3623a` and `46dbc60` suppress nothing the remaining rules miss; they are removed. Left: the `google-services.json` path and commits `b9e9065` (deleted Supabase client key) and `46e50fd` (one historical `local.properties`) |
| TD-48 | Privacy | A personal Gmail address was committed in this public repo (`PRODUCTION_READINESS_GATE.md:211`) | 1 | 3 | 1 | **20** | **Fixed** (docs-truth PR #155): the line is gone from the tree; git history keeps it |
| TD-49 | Docs | `CONTEXT.md` was 83 % changelog (lines 134–780) | 3 | 2 | 2 | **20** | **Fixed** (docs-truth PR #155; trimmed to the planned 140 lines on 2026-09-24): history lives in [CHANGELOG.md](CHANGELOG.md); rules already in `CLAUDE.md` (baselines, `targetSdk`, Kover, the backend lock, PR target) are no longer repeated in `CONTEXT.md` |
| TD-50 | Console | `seatRow` / `inviteRow` exist in both `operator.js:661/678` and `institution.js:115/141` and have drifted: the operator desk shows an expired lease as "until …" | 2 | 2 | 2 | **16** | **Fixed** (console PR): `console/util.js` `seatCells` / `inviteCells` render the four shared columns for both pages (operator table reordered to Member · Status · Seat · Device); an expired lease reads "—". `firebase-hosting/tests/util.test.mjs` |
| TD-51 | Code | Dead code: `CloudRestore.listRestorable` / `listRestorableSessions` and their 60 s cache (`data/CloudRestore.kt:209-277`, no caller); six `SessionStore.*Async` wrappers; `FrameOrderHelper.loadMeta/sortMeta`; a doc comment + `@Suppress` orphaned above the wrong function (`CloudRestore.kt:527-532`); worker keys `KEY_DONE/KEY_TOTAL/KEY_LOCAL_ID` never read; `SettingsActivity.kt:530` reads `KEY_CLOUD_SESSION_ID` from progress no worker writes; `AnalysisViewModel.hasCompletedAnalysis` write-only | 2 | 1 | 1 | **15** | **Fixed** (app-correctness PR), except `hasCompletedAnalysis`, which goes with [ADR-004](../adr/ADR-004-runspec.md): the dead listing, its cache and invalidation, six wrappers, `loadMeta` / `sortMeta` plus the now-unused URI date probes, the orphaned doc, unread worker keys and the `SettingsActivity` progress read |
| TD-52 | Backend | `notify._retry_delay` honours `Retry-After` with no upper limit (`backend/app/notify.py:92-100`): one 429 can park the single notify worker | 1 | 2 | 1 | **15** | **Fixed** (backend quick-wins PR): `app/backoff.retry_delay` caps `Retry-After` at 16 s (the exponential ceiling) for both notify and Drive; Drive's copy, on the request path, had the same unbounded wait |
| TD-53 | Backend | The contention block `try tx / except Exception: if not _lost_to_contention(exc): raise` is repeated ten times in `firestore_repo.py` | 2 | 2 | 2 | **16** | **Fixed** (backend refactor PR): one `_run_tx(body, *, on_contended)` in `firestore_repo.py` runs every transactional body; each of the ten sites passes what a contention loss means there (`_CONTENDED`, `False`, a re-read, …). `bind_device_lock` keeps its outer re-read loop around it. Unblocks [ADR-001](../adr/ADR-001-firestore-repo-package.md) |
| TD-54 | Backend | Router duplication: `raise HTTPException(429, …)` ×35 at audit; #154 moved the 25 signed routes to the `deps.rate_limited` dependency (so a 429 no longer spends the nonce), leaving **14** in-handler raises (`admin` ×4, `devices` ×2, `health` ×2, `institutions` ×1, `licenses` ×3, `sessions` ×2) that send no `Retry-After`; `routers/sessions.py` ownership guard ×4, page-size clamp ×3 with two ceilings, `page` dict ×3; cursor loop copied (`firestore_repo.py:3147,3175`); 15 raw status strings bypass `statuses.py`; two `_retry_delay`s (merged into `backend/app/backoff.py` in the quick-wins PR); `sessions.py:21` imports from another router | 2 | 1 | 2 | **12** | **Fixed** (backend refactor PR): `rate_limit.enforce(bucket, key)` raises the 429 with `Retry-After`, and `deps.rate_limited` uses it too, so the 14 unsigned routes now say when to retry (`test_an_unsigned_429_says_when_to_retry`; the signed-route guard also catches `enforce` in a handler). `routers/_shared.py` holds `json_dumps`, `clamp_page_size` and `page_block` (used by `sessions` and `admin`), and `sessions._owned_session` is the one ownership guard. `_cursor_page` and `_session_file_docs` replace six copied paging loops in `firestore_repo.py`. The 13 raw status literals use `statuses.*` (the `validation.AccessStatus` `Literal` stays literal) |
| TD-55 | Test | Emulator re-race logic in three variants (`test_firestore_emulator_integration.py:541-570, 600-620, 706-721`); the `store` fixture defined in 10 files; `test_licenses.py` is 2,518 lines | 1 | 2 | 2 | **12** | **Fixed** (backend refactor PR): `_race_until(race, settled)` in the emulator tier; each test's per-round assertions live in its `settled`, and the device-lock test re-races a fully starved round like the other two. `store` and `audited` live in `tests/conftest.py`, and the six modules that need more override `store` by taking it. `test_licenses.py` is seven files by section, plus `tests/license_helpers.py` for the four helpers they share; an `ast.dump` comparison showed every test body moved unchanged |
| TD-56 | CI | Copy-paste across workflows (java+gradle setup ×6, KVM ×2, ccache env ×3, prune ×3, arm64 `.so` check ×2); `.github/actions/dic-submodules` referenced nowhere; six jobs without `timeout-minutes` | 2 | 1 | 2 | **12** | **Fixed** (CI composites PR): `setup-android-build`, `enable-kvm`, `check-arm64-so`, `backend-gate`; the `CCACHE_*` env moved into `setup-native-ci`; `dic-submodules` deleted; `timeout-minutes` on `ci-ok`, Deploy's `test`/`deploy`, Release's `publish` and both `backend-lock` jobs |
| TD-57 | Test | 14 main classes over 300 lines with no test at all (`ShareCenter` 759, `SweepSetupHelper` 822, `DicBatchRunner.kt` 413, `SessionUploadBundler` 397, `PdfReportGenerator` 304, `VsgPlotView`, `StudioOverlayView`, `TouchImageView`, `RoiDrawActivity`, `VsgLatticeView`, `SessionSelectionController`, `MediaPickerSheet`, `ViewerSettingsSheet`, `SessionListAdapter`); `HotPathMicroBenchmark` never runs in CI (`ci.yml:371`) | 3 | 3 | 4 | **12** | **Fixed** (coverage PR + view-class PR). Logic classes, JVM: `PdfReportGeneratorTest` (progress, Error on a throw), `SessionUploadBundlerTest` (progress ticks, CSV rows, skipped reports), `SweepSetupHelperTest` (sweep frame, ROI-capped plan), `DicBatchRunnerLimitTest` (quota stop before the engine), `ShareCenterTest` (save-to-Files CSV from a real viewer, failure pill, `stepAt`). Device: `PdfReportDeviceTest`, `SessionUploadBundlerDeviceTest` (Robolectric's PdfDocument cannot start a page). View classes, Robolectric: `VsgPlotViewTest` (scrub interpolation and clamping, muted curves left out, pinch floor at 5 %, pan clamp, double-tap reset, export size), `TouchImageViewTest` (rest fit in the chrome-safe box, ROI fit, double-tap 2x and back, pan clamp, pinch 10x ceiling and whole-specimen floor, swipe/fling scrub, downsampled bitmap), `StudioOverlayViewTest` (typed and drawn ROI in image px, clamping, corner/square resize, holes, letterbox remap, restore, ALPHA_8 mask), `RoiDrawActivityTest` (result rect + mask file, typed ROI/hole on a RAW preview, refusals, modes across a recreate), `VsgLatticeViewTest` (node hit-testing, framed VSG range, double-tap / long-press, passive mode), `SessionSelectionControllerTest` (bar swap, select-all box, vanished rows, which delete prompt), `SessionListAdapterTest` (subtitle for short runs and sweeps, badges, progress bar, per-row rebinds), `MediaPickerSheetTest` (permission path, Files hand-off, multi-select Use, reference hint gate, videos only on Home), `ViewerSettingsSheetTest` (rows per frame and sweep combination, stop rows, line-cut legend). No production code changed. `HotPathMicroBenchmark` now runs in `tier-benchmark` (`ci.yml`, label `benchmark` / `run_benchmark`) after the Macrobenchmarks on the same emulator, `class=` filter, `DEBUGGABLE` suppressed, JSON uploaded as `microbenchmark-results`; tier 3 still excludes the package. Kover floor 27 → 37 → 49 (51.7 % measured, from 39.2 %) |
| TD-58 | Architecture | `backend/app/firestore_repo.py` is 3,432 lines / 138 functions, two thirds licensing | 3 | 2 | 4 | **10** | **Fixed** ([ADR-001](../adr/ADR-001-firestore-repo-package.md) PR): eleven modules under `backend/app/repo/` (largest `licensing.py`, about 1,300 lines); `firestore_repo.py` is a facade whose assignments propagate into the package, so no test changed. An `ast.dump` comparison showed all 159 definitions unchanged apart from `firestore` → `_base.firestore`; `tests/test_repo_facade.py` pins the propagation, the one-way imports and the facade's public names |
| TD-59 | Hygiene | Eight remote branches merged into `main` (`git branch -r --merged origin/main`, 2026-09-24): `beta-v0.1`, `beta-v1.2.0`, `damodar`, `video-decoding`, `chore/semper-names`, `docs/perf-142-deployed`, `fix/appcheck-real-phones`, `fix/rate-limit-before-nonce`. `fix/capture-dic-good-practice-gates` is **not** merged: 16 commits (tip `b4be524`) that no tag or other branch holds, and the `archive/*` tags exist only locally | 1 | 1 | 1 | **10** | **Fixed** (2026-09-24): the eight merged branches are deleted from `origin`; `git branch -r --merged origin/main` lists nothing else. The capture branch was tagged `archive/fix/capture-dic-good-practice-gates` (→ `b4be524`, pushed) and then deleted. The local-only `.claude/worktrees/agent-a0509767…` is not in the repo and stays a local cleanup |
| TD-60 | Build | Java 11 source/target (`app/build.gradle.kts:255-256`) vs JDK 17 in CI and detekt `jvmTarget = "17"` (`:366`); plugin versions outside the catalog; ktlint `1.5.0` hard-coded twice; `localProperty()` re-implemented for two keys | 1 | 1 | 2 | **8** | **Fixed** (app-reuse PR): Java 17 source/target in `app` and `benchmark`; detekt, Kover, Spotless, google-services and Crashlytics resolve through `gradle/libs.versions.toml` `[plugins]`; ktlint is `libs.versions.ktlint`; `INDIC_API_BASE_URL` and `INDIC_API_CERT_PINS` read through `localProperty()` |
| TD-62 | Misc | `StudioOverlayView` suppresses `DrawAllocation` over real `RectF` allocations in `onDraw` (:497, :500); `Thread.sleep` in the `RetryOnTransient` interceptor; `create_session` drops the next-page token (`routers/sessions.py:401,469`); `TIMEOUT_US` declared twice; `reconcile_institution_seats` reads one user per seat serially | 1 | 1 | 1 | **10** | **Fixed.** Backend in the backend quick-wins PR: `create_session` returns `nextPageToken` (one page is the whole manifest while `MAX_FILES_PER_SESSION` ≤ `_LIST_SOFT_LIMIT`, now a test), and `reconcile_institution_seats` reads holders with one `get_all`. App in the app-reuse PR: `StudioOverlayView` reuses a scratch `RectF` and no longer suppresses `DrawAllocation`; `RetryOnTransient` waits in 100 ms slices and ends the wait when the OkHttp call is cancelled (`RetryOnTransientTest`). Coroutine cancellation still does not reach it, because `IndicApi` runs a blocking `execute()` (the ADR-002 seam). `HardwareVideoDecoder.TIMEOUT_US` is the one declaration and `AviCodecDecoder` uses it |
| TD-63 | Code | `PendingApprovalActivity:53` and `SessionLimitActivity:69` constructed `DeviceKeyManager` — which loads the AndroidKeyStore and may generate a key — only to show or mail the device id, which needs neither; a Keystore fault crashed the screen that tells the user how to reach support | 2 | 2 | 1 | **20** | **Fixed** (app-correctness PR): Keystore-free `DeviceKeyManager.deviceId(context)` at every id-only site |
| TD-64 | Architecture | `backend/app/repo/licensing.py` is about 1,300 lines after the ADR-001 split (mint, claims, invites, entitlement, licence admin), past the ~1,000 the ADR set for splitting it again | 2 | 1 | 3 | **9** | **Fixed** (licensing-split PR): seven modules of 131–371 lines — `claims` (`claim_seat` `backend/app/repo/claims.py:117`), `invites`, `mint` (`backend/app/repo/mint.py:167`), `activation`, `entitlement` (`backend/app/repo/entitlement.py:123`), `license_admin`, `institution_admin`. Claims and invites import only `_base`; mint builds on both; activation, entitlement and the two admin modules sit on top. `licensing.py` only re-exports the 44 names it used to define; the facade and `devices`, `users`, `seats` import from the owners (`backend/app/firestore_repo.py:172`). An `ast.dump` and source-text comparison showed all 44 definitions unchanged, and every global each one reads resolves to the same object. `tests/test_repo_facade.py:106-149` pins each module's allowed imports and that `licensing.py` defines nothing |
| TD-65 | **Accuracy** (engine) | Two analyses of the same inputs with the same APK on the same emulator (`Pixel_8`, API 34, x86_64) do not give bit-identical results. Over three runs of one fixture (`steel_24` → `steel_26`, `steel_27`; subset 21, step 5, window 15), the `.dat` SHA-256 differed every time; about 1.8 % of points differed (25,777 / 26,243 bit-identical), with max \|Δu\| 0.0015–0.0031 px and \|Δexx\| 1.9×10⁻⁴, and the solved-point count moved by 1–2. The engine's contract calls any run-to-run variation a defect (`native/docs/TESTING.md:101-103`). A `.dat` hash can therefore not prove an app change left the engine alone | 3 | 4 | 3 | **21** | Open, engine repo. First check whether a single-threaded run (OpenCV `setNumThreads(1)`, engine workers 1) is reproducible, then bisect seeding vs propagation. Not yet checked on a physical device |
| TD-66 | Data | The upload's `csv/analysis_data.csv` interleaves sections: `SessionUploadBundler` calls `appendFieldStats` then `append` per frame (`data/SessionUploadBundler.kt:164-165`), so frame 2 onward's `# ` field-stats rows land among the point rows. The viewer's Share CSV writes every frame's stats first (`report/AnalysisCsvWriter.kt:64-71`). A reader that skips `#` lines parses both; one that splits on the first blank line does not | 1 | 2 | 2 | **12** | **Fixed** (2026-09-24): the appender holds stats rows in memory and stages point rows appended before the point section in a sibling `.points.tmp`, then writes stats, header and points at close (`report/AnalysisCsvWriter.kt:198-235`); still one decode per frame. `results/AnalysisCsvSectionsTest` checks the one-pass file equals the Share file byte for byte |
| TD-67 | **Data** | A failed PDF write reported success. `PdfDocument.writeTo` drops an `IOException` from its stream (the native writer stops and the call returns normally), so `PdfReportGenerator` emitted `Complete` over a truncated file on a full disk or a revoked SAF document. Found by `PdfReportDeviceTest.aFailingStreamEndsInError` on the Pixel_8 emulator | 2 | 3 | 1 | **25** | **Fixed** (coverage PR): `writeChecked` wraps the stream and rethrows its first failure, so the flow ends in `Error` (`report/PdfReportGenerator.kt`) |
| TD-68 | CI | Dependabot docker PRs run gates only: `ci.yml:236-243` maps `dependabot/pip/*` to Tier 4 and `dependabot/gradle/*` to Tier 1, but `dependabot/docker/*` falls to the "unknown ecosystem" branch, so #172 (`python:3.12-slim` → `3.14-slim`) went green with no test at all. The docker entry (`.github/dependabot.yml:52`, added under TD-46) also offers major/minor Python bumps, although the lock is compiled on 3.12 and the stated aim was OS fixes. Fix: `ignore` minor/major `python` updates there so only digest/patch bumps arrive, and map `dependabot/docker/*` to Tier 4 (which still runs on the runner's Python, not the image) | 2 | 3 | 1 | **25** | **Fixed** (2026-09-24): `dependabot.yml` ignores `python` minor/major updates for the image, `ci.yml` maps `dependabot/docker/*` to Tier 4, and #172 was closed |
| TD-69 | Build | Every Gradle run warns "Deprecated Gradle features were used … incompatible with Gradle 10". `--warning-mode all --stacktrace` traces it to one call: `ReportingExtension.file(String)` from `io.gitlab.arturbosch.detekt.DetektPlugin.apply` (`DetektPlugin.kt:28`), detekt `1.23.8` (`gradle/libs.versions.toml:30`); nothing in our build scripts. Gradle 10 removes the method, so the wrapper cannot move to 10 until detekt ships a fix | 1 | 2 | 1 | **15** | Open (found 2026-09-24): wait for a detekt release that drops the call; Dependabot's gradle group proposes it. Do not bump the wrapper to 10 before then. Re-checked 2026-09-24: Maven Central's newest 1.x is still `1.23.8`; the only newer line is detekt 2.0 under `dev.detekt`, at `2.0.0-alpha.6` |
| TD-70 | Ops | Firestore rules and indexes could not be deployed as documented. `firebase-hosting/firebase.json` pointed its `firestore` block at `../firestore.rules` and `../backend/firestore.indexes.json`, and the CLI refuses files outside its project directory ("is outside of project directory", 2026-09-24, first attempt to deploy the rules for gate row :25). The block also defaulted to the Auth project, not the backend project that holds Firestore. `test_security_controls.py` asserted the broken path | 2 | 3 | 1 | **25** | **Fixed** (2026-09-24): the block is gone; `scripts/deploy-firestore.sh [rules|indexes]` stages both files beside a `firebase.json` of their own in a scratch directory, defaults to `indicvision-dic-app`, and refuses rules that are not deny-all; the test now asserts the script and that Hosting's config has no `firestore` block |
| TD-71 | Security | The deploy SA `indic-deployer` holds two project-wide roles broader than a deploy needs (IAM policy read 2026-09-24). `roles/storage.admin` is not in the documented grant (`CLOUD_ARCHITECTURE_GCP.md:737-740` lists `run.admin`, `artifactregistry.writer`, `iam.serviceAccountUser`, `cloudbuild.builds.editor`), probably added for the source upload of `deploy-cloudrun` (`source: backend`, `deploy-backend.yml:219`); it also lets CI overwrite or delete the Firestore export bucket. `roles/iam.serviceAccountUser` at project level lets CI act as every SA, `firebase-adminsdk-fbsvc` (token creator) included. Reachable only through a compromised WIF deploy identity | 2 | 3 | 2 | **20** | **Fixed** 2026-09-25 (IAM, no code). The plan was: check the backup bucket's soft-delete/retention first; then grant `serviceAccountUser` on `indic-api`, `indic-gw` and the Cloud Build SA only and `storage.admin` on the `run-sources-*` bucket only, prove it with a staging deploy and a production `dry-run`, and only then drop the project-level bindings. Record the grant in `CLOUD_ARCHITECTURE_GCP.md`. **Progress, re-read 2026-09-24 17:50Z:** the project-level roles are now `apigateway.admin`, `artifactregistry.writer`, `cloudbuild.builds.editor`, `run.admin`, with no `storage.admin` and no `iam.serviceAccountUser`. `storage.admin` and `storage.objectAdmin` sit on the `run-sources-indicvision-dic-app-asia-south1` bucket only. The backups bucket keeps a 7-day soft delete and has no retention lock. **2026-09-25:** the per-SA `serviceAccountUser` grants are on `indic-api`, `indic-gw` and the default compute SA only. Staging deploys failed from the clean-up on (`403 … storage.buckets.list`: a source deploy lists the project's buckets to find `run-sources-*`), so `roles/storage.bucketViewer` (buckets get/list, no object access) was added at project level; staging then deployed (run 36095277741). The grant is recorded in `CLOUD_ARCHITECTURE_GCP.md` §9, and production deployed on the narrowed roles too (run 36096112375) |
| TD-72 | App | A fast swipe in the viewer stepped two frames. The same ACTION_UP is both a fling (`TouchImageView.onFling`, `ui/viewer/TouchImageView.kt:351`) and a swipe past `SWIPE_DISTANCE` (`maybeFitSwipe`, `:161`, called at `:128`), and each called `onScrubListener`. Seen on the emulator: one flick moved 4 → 6 | 2 | 1 | 1 | **15** | **Fixed** (TD-57 PR): one scrub per gesture (`scrubbedThisGesture`, reset on ACTION_DOWN); `TouchImageViewTest` "a fast long swipe … steps one frame" failed before (`[1, 1, -1, -1]`) |
| TD-73 | **Accuracy** | A typed ROI could reach the engine a pixel low. The overlay keeps the ROI in view pixels, so a typed 1000 came back as 999.9997, and `RoiDrawActivity.saveAndFinish` truncated it while the HUD rounded. Estimated from float32 on a 4032-wide photo: about 28 % of x values at a 1080 px wide view, 71 % at 1440 px | 3 | 2 | 1 | **25** | **Fixed** (TD-57 PR): `roiPixels` (`ui/analysis/RoiDrawActivity.kt:427`) rounds the edges and clips to the image; `RoiDrawActivityTest` covers the n − ε case and the clip. A drawn ROI can also move by ≤ 1 px |
| TD-74 | App | `StudioOverlayMaskEncoder.encode` leaves the ALPHA_8 background opaque (`drawColor(Color.BLACK)`), so only holes are void and the ROI rect alone bounds the crop. That is correct for a rectangle and wrong for the circle / ellipse / freeform modes the encoder also drew, which no tool could reach (`RoiDrawActivity.kt:190-193`) | 1 | 2 | 1 | **15** | **Fixed** (TD-74 PR), not as first proposed. Clearing the background would void a subset radius around every crop: the engine drops any point whose subset touches a void pixel (`native/src/pipeline/full_field_solver.cpp:166-170`). So the background stays opaque, the unreachable modes are deleted (`StudioOverlayView.RoiMode` is `RECTANGLE`, `SQUARE`), the encoder draws only holes, and `StudioOverlayViewTest` asserts the outside reads 255. Mask bytes are unchanged: the crop and full-image fills it drops painted 255 on 255 |
| TD-75 | Performance | `VisualizationEngine.quickSelect` (`report/VisualizationEngine.kt:136`, also used by `ReportBuilder.kt:155`) went quadratic on repeated values: its Lomuto partition moved only values strictly below the pivot, so each pass settled one copy. 300 000 equal values took 69.9 s on the JVM; `HotPathMicroBenchmark` never finished `buildReport` / `valueRanges_150frames` (reported by the material_testing session, 2026-09-24) | 3 | 3 | 1 | **30** | **Fixed**: three-way partition settles every copy of the pivot in one pass (0.06 s) and returns the same element a full sort would; `VisualizationEngineTest` bounds the time and checks both p02/p98 picks against a sort on duplicate-heavy fields |
| TD-76 | Performance | After TD-75, `quickSelect` (`report/VisualizationEngine.kt:139`) still went superlinear on sorted and staircase fields: its median-of-three pivot (ends and middle) stops being representative after the first partition, so the p98 pick touched about n^1.5 elements (~370n, 0.8 s on the JVM at 1.2 M points). A V field in grid order is a staircase. `HotPathMicroBenchmark.valueRanges` measured 14.9 ms against the 1.15 ms the full sort it replaced took (`docs/perf/round2-main-vs-branch.md`); 3c88922e swapped the sort out without a benchmark (found and fixed in material_testing#25, 2026-09-24) | 2 | 3 | 1 | **25** | **Fixed**: Tukey's ninther for the pivot (`VisualizationEngine.kt:182`) keeps every shape tried near 4n, and an introselect cap sorts the rest of the range after 2·log2(n) passes, bounding the worst case at O(n log n). The picks stay those of a full sort (400 000 fuzzed picks); `VisualizationEngineTest` bounds sorted/staircase time and covers the fallback. On the API 37 emulator (debug), `valueRanges` went 14.9 → 4.6 ms, ×150 2.24 → 0.74 s, `buildReport` 121 → 89 ms. The benchmark's U/V columns are exact ramps, which a sort finishes in one run-detecting pass; on real session frames quickSelect is 2–4× faster than the sort |
| TD-77 | App | PDF reports came out as 87.5 × 123.8 cm pages: `PdfLayoutEngine` (`report/PdfLayoutEngine.kt:23`) passed its 2480 × 3508 layout size (A4 at 300 dpi) to `PdfDocument.PageInfo`, which takes PDF points (1/72 in). Viewers and fit-to-page printing scale it down, so it looked right; printed at actual size it ran 4.2× A4 each way, and PDF tools reported a non-standard paper size (found in material_testing on the emulator, 2026-09-24) | 2 | 2 | 1 | **20** | **Fixed**: `report/PdfA4` starts 595 × 842 pt pages and scales the canvas by 595/2480, so the layout keeps its design units; text now reads 8.6–13.9 pt. `PdfReportDeviceTest` asserts every MediaBox is `0 0 595 842` (ported from material_testing#27) |
| TD-79 | App | A disabled primary button looked enabled: `Widget.Semper.Button` and `Widget.Semper.Button.Cta` (`res/values/styles.xml:24`, `:56`) tinted every state `sky_primary`, with plain white text. Only the wizard's Next, Compute and Run sweep faded themselves by hand (`view.alpha = 0.4f` in `AnalysisReadyGate` and `SweepSetupHelper`); the ROI studio, the lattice and sign-in did not (found on the material_testing fork's emulator pass, 2026-09-24; TD-78 there is a lab-only row) | 2 | 1 | 1 | **15** | **Fixed**: the base style takes `color/button_primary_bg` (grey `surface_outline` while disabled) and the new `color/button_primary_text` (`text_secondary` while disabled); Cta inherits both, and the hand-set alphas are gone. `ui/common/PrimaryButtonStyleTest` checks the theme default and the ROI studio's Save |
| TD-80 | App | The sweep lattice's scrub slider drifted from the plot: `VsgPlotView.clearScrub` (`ui/analysis/VsgPlotView.kt:696`) cleared the guide on lift and on a pinch but told only `onScrub`, so the slider (`VsgLatticeActivity.kt:233`) stayed where the finger left it. A pinch or double-tap starts with a one-finger DOWN that scrubs, so either one moved the slider and left it there (found on the material_testing fork's emulator pass, 2026-09-24) | 1 | 2 | 1 | **15** | **Fixed**: `clearScrub` also reports NaN to `onScrubMove`, and the lattice puts the slider back to 0, the rest position a new node already gives it. `VsgPlotViewTest` covers lift, pinch and double-tap |
| TD-81 | App | A single run that failed outright left Compute disabled for good. Compute is disabled when a run starts and only `checkReady()` turns it back on; `BatchRunController.handleBatchOutcome` called it in every branch except `showNamedEngineFailure` (`BatchRunController.kt:137`), the one taken when frame 0 fails with an engine code (−1 features, −2 ROI, −3 init) or yields 0 points (code 0, the "VSG failure" message). The settings sheet's `onParamsChanged` only clears the status line (`StaticAnalysisActivity.kt:1053`, `:1493`), so changing the strain window, subset, step, overlap, kernel, Reset or Paste could not bring Compute back; Back → Next, a new ROI or new images could. Reproduced on the emulator 2026-09-25 (subset 41, step 20, window 31 on the DICe 512 px pair: ICGN solved 441 points, VSG kept 0) | 3 | 3 | 1 | **30** | **Fixed** (2026-09-25): `handleBatchOutcome` calls `checkReady()` once, before any branch (`BatchRunController.kt:96`), and the per-branch calls are gone. `BatchRunControllerTest` covers every single-run ending and fails without the fix; on the emulator the same run re-ran with window 5 (357 points). The same dialog blamed the strain window for every code-0 run; the outcome now carries the points ICGN accepted on the first frame (`BatchAnalysisOutcome.firstFrameCorrelatedPoints`, engine metric slots 3 + 4), and `EngineFailure.zeroPointsMessage` names the run's VSG and step when points correlated, says nothing correlated when none did, and reports an unreadable frame when the engine never ran on it |
| TD-82 | App | Nine plurals hard-coded numbers in their "one" form: the Home chip read "1 / 1 analyses used" under a cap of 999 (`res/values/strings.xml:891`), a one-combination sweep read "subset 1–1 px · window 1–1 points" (`:911`), and the limit screen, lattice summary, sweep headline and partial-delete messages did the same. Two more said "analysis(es)" / "backup(s)", and the PDF share row had a stray " . " (`:939`) (found on the material_testing fork's emulator pass, 2026-09-24; TD-81 there is a lab-only row) | 2 | 1 | 1 | **15** | **Fixed**: every "one" form formats its arguments, the cloud prompts agree in number, and the row uses "·". `ui/common/QuantityCopyTest` fails if a "one" form drops more than the count it is chosen by |
| TD-83 | App | A dropped frame thumbnail stayed 6% enlarged: the drop starts a 120 ms shrink, then `FrameOrderAdapter.refreshBadges` (`ui/analysis/FrameOrderAdapter.kt:66`) posts a payload change, and the item animator ends that holder's running animation, freezing it at 1.06× (found on the material_testing fork's emulator pass, 2026-09-24) | 1 | 1 | 1 | **10** | **Fixed**: the badge-only bind (`:47`) cancels the animation and puts the tile at rest. `FrameOrderAdapterTest` |
| TD-84 | App | The custom colour-scale dialog opened empty unless a custom scale was already set (`ResultViewerActivity.showCustomScaleDialog`, `ui/viewer/ResultViewerActivity.kt:852`), though WORKFLOWS 8.1.7 says it is prefilled with the current bounds; custom values were written with `Float.toString` (found on the material_testing fork's emulator pass, 2026-09-24) | 1 | 1 | 1 | **10** | **Fixed**: `ui/viewer/CustomScalePrefill` fills it with the custom bounds, else the auto bounds the bar shows, formatted as the bar formats them, so Apply with no edits keeps the scale. `CustomScalePrefillTest` |
| TD-85 | App | A plot tick could read "-0.00": `VsgPlotView.format` printed an axis end a hair below zero with its sign; the fork saw it on the load–deflection plot (found on the material_testing fork's emulator pass, 2026-09-24) | 1 | 1 | 1 | **10** | **Fixed**: `VsgPlotView.tickLabel` (`ui/analysis/VsgPlotView.kt:79`) drops the sign from a label that rounds to zero. `VsgPlotViewTest` |
| TD-86 | CI | `HotPathMicroBenchmark` never ran in CI: all 10 cases failed in `BenchmarkRule`'s permission grant with `SecurityException: Package com.indicvision.semper has not requested permission android.permission.WRITE_EXTERNAL_STORAGE`. The micro run is `:app`'s androidTest (`.github/workflows/ci.yml:649`), so the grant goes to the app, whose manifest requests the permission only up to API 32 (`app/src/main/AndroidManifest.xml:6`); the CI emulator is API 34. #194's own PR run skipped the benchmark job, so the first run was the dispatched full run 36082724061 on #197 (2026-09-25) | 2 | 1 | 1 | **15** | **Fixed**: the debug manifest (`app/src/debug/AndroidManifest.xml`) drops `maxSdkVersion` from that one permission, so the grant succeeds; release and benchmark builds keep the API 32 cap. Past the grant, every case failed on `ACTIVITY-MISSING DEBUGGABLE NOT-AOT-COMPILED` with only `EMULATOR` suppressed: `suppressErrors` passed through Gradle arrives cut at its first comma. CI now installs the APKs and runs `adb shell am instrument` with all six suppressions, fails the step unless it prints `OK (`, and pulls the JSON from the device |
| TD-87 | Performance | `ViewerScrubBenchmark.scrub150Frames` max heap went from ~51 MB to ~161 MB after TD-75 (three runs, 161,348–161,790 KB median; `scrub10Frames` unchanged at 27.6 MB). Not a leak: the viewer starts the colour-scale pass on every multi-frame open (`ui/viewer/ResultViewerActivity.kt:358`, since 73ef47d6), and the benchmark's seeded session had no `FieldRangesStore` sidecar (`DicBatchRunner.kt:292` writes one for real batches), so `SummaryAnimation.globalRanges` (`ui/viewer/SummaryAnimation.kt:298`) decoded and range-scanned all 150 frames: a new frame `FloatArray` (600 KB) and five columns (375 KB) each, ~150 MB of garbage in about a second. Before TD-75 the seeded strain fields (9, 6 and 4 distinct values) cost the old `quickSelect` ~35.9 M comparisons per frame against 0.41 M now (87×, simulated), so the pass never got far inside the measured window. 1313eb2d had measured the same pass at +340% heap at 150 frames. `ViewerSummaryHelper.start()`'s KDoc still said the pass ran only on demand from `show()` | 2 | 2 | 2 | **16** | **Fixed**: `BenchmarkSeedActivity` writes the sidecar, so the benchmark measures the path a real session takes (#208: `scrub150Frames` max heap 161,790 → 21,280 KB, `scrub10Frames` 27,661 → 17,702 KB), and the KDoc says when the pass runs. The no-sidecar fallback (older or restored sessions, `SessionUploadBundler.kt:262`) now decodes into one reused buffer (`DatDecoder.decodeInto`) and one set of columns (`VisualizationEngine.valueRanges` with a float count and scratch), allocating again only for a frame larger than any before; `DicResultDecodeTest`, `VisualizationEngineTest` and `SummaryAnimationTest` check reuse, a stale tail and mixed frame sizes against a fresh decode |
| TD-88 | Performance | The no-sidecar colour-scale pass (TD-87) never saved what it computed: `SummaryAnimation.globalRanges` (`ui/viewer/SummaryAnimation.kt:273`) decoded every frame when `field_ranges.bin` was missing or stale and returned the ranges without writing them, and only `DicBatchRunner` (`ui/analysis/DicBatchRunner.kt:296`) writes the sidecar. A restored or older session therefore paid the full decode on every viewer open (`ui/viewer/ResultViewerActivity.kt:358`) and every backup (`data/SessionUploadBundler.kt:262`). `ScreenBenchmark.settingsScroll` also failed on the tall (1080×2424) API 37 image: flings overshot "Your data", and a held `UiObject2` went stale after a section expanded (ported from material_testing#34, 2026-09-25) | 2 | 2 | 1 | **20** | **Fixed**: a full decode in which every frame reads saves its per-frame ranges as the sidecar (`SummaryAnimation.saveRanges`, write-then-rename), so the pass runs once per session; a stale sidecar is replaced by the decode it forced, and a failed frame writes none. `ScreenBenchmark` finds each header from the top in half-screen steps and looks the list up again before every gesture. `SummaryAnimationTest` covers the write-back, the stale replacement and a failed frame. In material_testing: `scrub150Frames` 22.0 MB max heap on a Pixel 6 and 23.2 MB on the API 37 emulator; `settingsScroll` passes on both |
| TD-90 | App | A build with no `INDIC_API_BASE_URL` crashed on open when it held a signed-in, approved session: the background status check (`ui/auth/StatusRecheck.kt:32`, also `SplashActivity.kt:67`) reached `AuthRepository.resolveStatus` (`data/AuthRepository.kt:596`), which fetched a token without asking whether a backend was configured, and `IndicApi` built the request from an empty base (`data/net/IndicApi.kt:181`, `"$base/v1/me"`). OkHttp throws `IllegalArgumentException` on the bare `/v1/me`, not an `IOException`, so nothing caught it and the process died. Found by `StartupBenchmark` on a Pixel 6 (Android 17) in material_testing, 2026-09-25; CI's fresh emulator never has a session, so it never hit it | 2 | 2 | 1 | **20** | **Fixed**: `resolveStatus` treats "no backend configured" like offline (cached approval stands, no network call). `AuthRepositoryTest` "no backend configured is treated like offline" checks both outcomes and that `/v1/me` is never called. Every other `IndicApi` call had the same crash waiting (uploads, restores, licences, the session list): all URLs now go through `IndicApiHttp.endpoint`, which throws `IndicApi.CloudNotConfiguredException` (an `IOException`, so read as offline) for a blank base; `ApiErrorMappingTest` covers it. Ported from material_testing; TD-89 is material_testing's number for this repo's TD-81 |
| TD-97 | CI | Merges to `main` cancelled each other's CI, so on a busy day `main`'s full matrix never finished. `ci.yml:21` grouped runs per branch with `cancel-in-progress: true` for every event, `main` included; Tier 3 and Tier 5 take about 12 minutes, so any merge inside that window cancelled them. On 2026-09-26 the push runs of #268 (36237432020), #270 (36238372406) and #271 (36238631416) were all cancelled, so #271 never had a full run of its own; only #272's run finished. material_testing found and fixed the same under this number (material_testing#67) | 2 | 2 | 1 | **20** | **Fixed**: `cancel-in-progress: ${{ github.event_name == 'pull_request' }}` (`ci.yml:25`), so a PR's older run is still cancelled but a started `main` run finishes. GitHub keeps one pending run per group, so a burst of merges runs the one in flight and the newest; the commits in between get no run of their own ([CI.md](CI.md)) |
| TD-133 | App | material_testing ships under Semper's app id (`app/build.gradle.kts:90`, `applicationId = "com.indicvision.semper"`; material_testing's `app/build.gradle.kts:90` is the same), so installing its lab build replaces Semper on a phone and inherits Semper's analyses, sign-in, licence state and upload queue. The lab build has no API URL, so the analyses Semper had not yet backed up sat on "upload pending" (found 2026-09-26: 5 of 9 on the Pixel 6, 25 on the Samsung). This PR makes those rows read "Not backed up"; it does not stop the lab build taking over the install | 2 | 2 | 1 | **20** | Open. Give material_testing its own `applicationId` (for example `com.indicvision.semper.lab`), in that repo |
| TD-136 | Console | The account page could wipe its own "Could not read your account" line: `loadAccount` reported a failed `/v1/me`, but `loadSessions` runs in parallel and cleared the status line when it succeeded, so when the analyses answered second the failure disappeared. Found by #270's page tests. #271 recorded it as TD-134 and deleted the row with the fix; TD-134 is a lab row in material_testing, so it is renumbered here | 1 | 1 | 1 | **10** | **Fixed** (#271): the failure is kept in `accountError` (`firebase-hosting/public/console/account/account.js:44`) and the analyses load restores it instead of blanking the line (`account.js:223`); `account.test.mjs` covers that order and fails on the old code |

### Cannot be verified from the repository

Checked by hand in the console or with `gcloud`, not by reading code: whether
the legacy `indic-gw` / `indic-api` pair exists (TD-30); branch protection
requiring `CI OK`; API-key restrictions; GitHub Environment branch rules on `production`. The open
checkboxes are in [PRODUCTION_READINESS_GATE.md](PRODUCTION_READINESS_GATE.md).

### Closed as obsolete, 2026-09-14

TD-4, TD-5, TD-18, TD-20 and TD-21 all described the in-app camera: the
capture orchestrators, their missing instrumented tier, `GrayPngEncoder`,
`LockedCameraSession`'s duplicated bodies, and the per-capture `ImageReader`
re-registration. `449c9da` removed the feature, so `ui/capture/` is gone from
both this branch and `main` and none of those symbols resolves anywhere in
`app/src`. They are struck rather than carried as permanently deferred — a
register row that cannot be worked is noise. TD-23 is closed by work, not by
deletion: the revoke now stamps a check-in checkpoint the `lastSeenAt` throttle
honours.

**TD-32 is closed by `chore/prune-cand-tags`.** The promote now routes
`--to-latest` (refusing if the latest ready revision is not the candidate) and
removes every `cand-*` tag in the same `update-traffic` call, so tags no longer
pile up (one per revision per deploy) and a hand-run `gcloud run services
update` no longer creates a revision that serves 0 %.

**TD-31 is closed by #133.** The flaky emulator invite race was not asserting a
real guarantee: a round the emulator starves grants nothing, and with no grant
there is no grant for a loser to stamp a Demo key over. `_race_entitlement`
re-races such a round instead of failing on it — 32 consecutive runs, the retry
path firing in four of them. What that starvation costs an account outlives the
test and is now TD-33.

## External / deferred (not blocked on code alone)

| Item | Why deferred |
|------|----------------|
| Auth-gated UI E2E | Needs Firebase secrets / fixtures in CI |
| `ViewerArgs` read half | Now TD-3 / [ADR-003](../adr/ADR-003-viewerargs-read-side.md) |
| firebase-admin / hashed lock | Lock is regenerated from txt on each bump (`pip-compile --generate-hashes` on Python 3.12). Direct-dep versions in the lock must match `requirements.txt`. |
| Identity Platform upgrade | The consoles' second factor is Firebase MFA (TOTP), which needs the project upgraded to Identity Platform — a project-wide Auth change shared with the mobile app, and a change to the Auth pricing model. Until it is done the consoles sign in and every write fails `mfa_required`. |

## Perf / quality gates (do not loosen)

See [../engine/PERF_BASELINE_bd44af0.md](../engine/PERF_BASELINE_bd44af0.md):
≥ 4557 solves/s host; smoke/DICe floors; preserve `-O3 -ffast-math` / OpenMP / LTO
on the release pipeline. **No CI job in this repository enforces the solve-rate
floor** (that document says so at line 10): it is checked by hand in the engine
repo when the `native` pin moves. The Kover floor was report-only until TD-38;
`koverVerify` now runs in CI tier 1 and `ciReleaseGate`.

Macrobenchmark CI (`tier-benchmark`) is emulator **smoke**: it suppresses
`EMULATOR,LOW-BATTERY,UNLOCKED` and does not assert numeric thresholds. Keep API 34
(API 37 `dumpsys gfxinfo framestats` is empty). Dispatch with `run_benchmark` or
the `benchmark` label.

## User-facing consent copy (fixed 2026-08-24)

**Send crash reports** understated its scope: the same flag
(`DicSettings.diagnosticsEnabled`) gates `analytics/SemperAnalytics` as well as
Crashlytics. The toggle now reads **Send crash reports and usage data**, and its
subtitle and the first-run prompt name the usage events explicitly alongside what
is never sent. `docs/legal/PRIVACY_POLICY.md` §2.4 names the new label and the
hosted pages were regenerated. Nothing about *what* is collected changed — the
events were, and remain, PII-free buckets.

## 2026-08-12 result-viewer / report memory & latency program

Two rounds cutting per-point duplication and per-frame main-thread work in the viewer,
report and export paths. Every change was representation / allocation / threading /
resolution only — computed numbers are bit-identical (GIF bytes byte-identical), pinned
by 0-delta parity tests. Measured before/after, including the drawbacks, is in
[../perf/round2-main-vs-branch.md](../perf/round2-main-vs-branch.md).

**Fixed**

- Report/summary/heatmap collectors de-boxed onto reused primitive buffers; the GIF LZW
  string table is a flat `IntArray` instead of a per-pixel-boxing `HashMap`. Measured
  −99.6 % to −100 % allocations on those paths.
- `.dat` decode is memory-mapped (`decodeDatFile`); export sites no longer read the whole
  file into a second `ByteArray` first.
- Stats-strip and Max/Min extrema moved off the main thread and memoised per (frame, field).
- Viewer look-ahead is now byte-bounded and filled by a single serialized worker, and the
  whole-batch summary scan starts on demand — see the invariants in
  [../app/ARCHITECTURE.md](../app/ARCHITECTURE.md#memory--failure-invariants). Max heap
  during a 150-frame scrub went 165 MB → 24.5 MB and is now flat in workload size.

**Open / deliberately not done**

| Item | Why |
|---|---|
| `StartupTimingMetric` benchmarks on an API 37 emulator | `startActivityAndWait` confirms launches via `dumpsys gfxinfo framestats`, which returns empty there for every activity. `StartupBenchmark`/`ScreenBenchmark` need a physical device or an older image (a Pixel 6 on API 37 works: [perf/startup.md](../perf/startup.md)); `ViewerScrubBenchmark` avoids the API and runs |
| Float16 / ZNSSD quantisation for field data | Would change reported numbers; rejected under the bit-exactness requirement |
| In-memory X/Y compaction (derive coords from the grid) | Loss-less and worth ~25 %, but a larger change that also touches the native writer |

Do not split VisualizationEngine loops, GifEncoder LZW, ReportBuilder fusion,
`DicResult.decodeDatFile`, `DicUploadWorker.doWork`, `prefetchAround` /
`ScrubFrameCache`, or `PointSpatialIndex.build`.

## 2026-08-05 transparency / robustness audit

A whole-app + backend pass for silent failures, crashes, security and tech debt.
The branch was already strong; findings and fixes were small:

- **Crash guards (fixed).** The JNI full-field solve reads its direct output buffer
  back by the engine's returned point count. Added a bound: a count exceeding the
  ROI-grid buffer capacity is now treated as an engine failure instead of reading
  past the buffer (`DicFieldIo` / `VsgStudyRunner` once #70 lands; still inline on
  `main` until then). Closes the residual "JNI buffer" concern from the earlier
  Tier-0 list.
- **Fragile null-asserts (fixed).** `ShareCenter` replaced nine `snap!!` sites with
  a checked `requireSnapshot()` that fails the share job cleanly (snackbar) rather
  than NPE-crashing.
- **Silent restore failure (fixed).** A background restore that failed terminally
  was never surfaced; `SettingsActivity` now observes the `restore` work tag and
  shows the reason, mirroring the existing upload-failure surfacing on Home.
- **Verified clean (no change):** cleartext disabled, no local token storage, no
  hardcoded secrets, release signing wired to an env keystore, backend Drive query
  escaping + `parents` confused-deputy guard, and no PII in Crashlytics WARN/ERROR
  breadcrumbs (`CrashReportingTree`).

## History

Earlier burn-down (CI path filters, Hilt removal, engine Path A–C split, OkHttp 5,
FastAPI train, Analysis helpers, Kover floor 15→27, UseKtx/Plurals/Overdraw, capture
maintenance pass on PR #101, etc.) is in git history — do not re-open closed items
without new evidence.
